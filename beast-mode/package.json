{
  "name": "beast-mode",
  "version": "1.0.0",
  "private": true,
  "description": "Beast Mode fitness tracking dashboard",
  "scripts": {
    "start": "node server/index.js",
    "dev:server": "node server/index.js",
    "build": "cd client && npm run build",
    "postinstall": "cd client && npm install"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "pg": "^8.11.5"
  }
}
